# Large reference HTML pages — local only, not needed for CDN
src/pages/

# Screenshots — local reference only
screenshots/

# Compiled/hashed CSS bundles — not needed on GitHub
src/php/

# Local sandbox and HTML reference snapshots — not needed on GitHub
*.html

# Node / build tooling
node_modules/

# OS / editor
.DS_Store
Thumbs.db
*.swp

# HAR captures — may contain auth cookies/session tokens
*.har


# Claude Settings Files
.claude/